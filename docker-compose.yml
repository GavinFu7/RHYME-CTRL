version: '3.8'

services:
  rhyme-ctrl:
    build: .
    ports:
      - "5001:5001"
    volumes:
      - ./static/uploads:/app/static/uploads
      - ./data:/app/data
    restart: unless-stopped